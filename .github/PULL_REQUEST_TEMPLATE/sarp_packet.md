## External SARP Packet

This PR carries a SARP packet only. It does not authorize implementation,
promotion, merge, publication, operator push, or broader rollout.

## Packet

- Manifest:
- Primary archive:
- Bounded question:

## Required Checks

- [ ] Exactly one primary SARP packet/archive
- [ ] SARP and implementation links are visibly separated
- [ ] Manifest uses `mb.sarp_delivery.manifest.v1`
- [ ] Verdict, if included, uses `mb.github_connector.sarp_verdict.v1`
- [ ] No implementation authorization is claimed

