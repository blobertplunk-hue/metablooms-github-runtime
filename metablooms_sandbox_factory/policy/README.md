# Factory Policy

`active_scope_policy.json` records the default source-scope policy used by the
sandbox factory.

`exception_policy.schema.json` records the minimum data required for any
candidate-bound exception.

The Stage071N V4 harness still owns final analyzer semantics. These policy files
make the sandbox factory's inputs explicit and auditable.
